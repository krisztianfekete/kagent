# Code generated by weaver from telemetry/registry. DO NOT EDIT.
"""Attribute names and values of the kagent telemetry contract."""

from enum import StrEnum
from typing import Final
{%- for attr in ctx %}

{{ attr.key | replace(".", "_") | upper }}: Final = "{{ attr.key }}"
"""{{ attr.brief }}"""
{%- endfor %}
{%- for attr in ctx if attr.members %}


class {{ attr.pyclass }}(StrEnum):
    """Values of {{ attr.key | replace(".", "_") | upper }}."""
{% for member in attr.members %}
    {{ member.id | replace(".", "_") | upper }} = "{{ member.value }}"
    """{{ member.brief }}"""
{%- endfor %}
{%- endfor %}
{{ "" }}
